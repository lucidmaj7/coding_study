#include <stdio.h>
#include <string.h>

int gethabit(char* str, int len , int start, int limitCount)
{
    int habitlen =0;

    if(start == len-1)
    {
        return habitlen;
    }


    return habitlen;
}


int main(void)
{
    int ncase = 0;
    scanf("%d\n", &ncase);

    for(int i=0;i<ncase;i++)
    {
        int limitCount = 0;
        
        scanf("%d\n",&limitCount);
        char str[4000]={0,};
        scanf("%s\n", str);
        int len = strlen(str);


    }


    return 0;
}